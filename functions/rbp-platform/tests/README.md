# Tests Placeholder

This directory is reserved for consolidated test entry points.

Structure:

- `tests/backend/`
- `tests/frontend/`
- `tests/integration/`

Source-specific tests will remain with their source packages until migration/import steps occur.
