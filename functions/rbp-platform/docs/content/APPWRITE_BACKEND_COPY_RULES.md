# Appwrite Backend Copy Rules

- public pages must not promise disabled provisioning
- checkout copy must state QA test mode where relevant
- admin-only and protected routes must not be framed as public product pages
- application pages must describe interest registration, not launch or provisioning
